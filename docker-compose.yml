version: '3.4'

services:
  deosrc.moviestechnicaltest:
    image: ${DOCKER_REGISTRY-}deosrcmoviestechnicaltest
    build:
      context: .
      dockerfile: Deosrc.MoviesTechnicalTest/Dockerfile

  db:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=Developer1!
    ports:
      - 1401:1433
